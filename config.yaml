subject_id: S004
experiment_protocol: pilot105
session_id: 1
image_set_label: image_b_pilot105_v1
task_mode: image_b
device_type: brainco
hardware_dummy_mode: false
sfreq: 250.0
buffer_sec: 80.0
protocol:
  fixation_sec: 1.5
  default_video_sec: 8.0
  blank_sec: 1.0
  iti_sec: 2.0
  rating_sec: 15.0
  baseline_sec: 0.0
  trials_per_session: 500
  video_library_dir: video_library/selected_540_balanced_videos
  video_library_mode: local
  random_seed: 17
  # pilot105 only reads real photographs; formal500 only reads AIGC images.
  pilot_image_library_dir: image_library/pilot
  formal_image_library_dir: image_library/formal
  # Legacy fallback for older code paths.
  image_library_dir: image_library/pilot
  # New standalone PsychoPy protocols.
  images_per_subject: 500
  pilot_images_per_subject: 105
  images_per_experiment: 500
  block_size: 100
  pilot_block_size: 105
  image_rating_block_break_min_sec: 60.0
  image_rating_block_break_max_sec: 60.0
  image_repeat_block_break_min_sec: 30.0
  image_repeat_block_break_max_sec: 45.0
  # Legacy Streamlit multi-repeat settings (not used by the standalone runner).
  image_unique_count: 4
  image_repeats: 5
  attention_probability: 0.05
  image_attention_sec: 2.0
  image_fixation_min_sec: 0.5
  image_fixation_max_sec: 0.8
  image_present_min_sec: 1.0
  image_present_max_sec: 1.5
  image_blank_sec: 0.5
  image_rating_item_min_sec: 3.0
  image_rating_item_max_sec: 4.0
device:
  neuracle_host: 127.0.0.1
  neuracle_port: 8712
  brainco_addr: ''
  brainco_port: 0
  brainco_auto_discover: true
  brainco_scan_timeout_sec: 6.0
  brainco_ready_timeout_sec: 20.0
  brainco_start_retries: 2
  brainco_gain: 6
  brainco_signal_source: NORMAL
  brainco_device_id: eeg-cap
  # bcigo: BCIGo records EEG/EDF and consumes our LSL markers (recommended).
  # lsl: only use if a separate tool really publishes an EEG LSL Outlet; sdk: direct device access.
  brainco_transport: bcigo
  bcigo_marker_wait_timeout_sec: 60.0
  brainco_lsl_stream_name: ''
  brainco_lsl_stream_type: EEG
  brainco_lsl_source_id: ''
  brainco_lsl_resolve_timeout_sec: 15.0
  brainco_lsl_ready_timeout_sec: 10.0
  lsl_marker_enabled: true
  lsl_marker_stream_name: visual-video-task-Markers
  lsl_marker_stream_type: Markers
  lsl_marker_source_id: visual-video-task-marker
  trigger_serial_port: ''
storage:
  records_dir: records_storage
  ratings_dir: ratings_storage
