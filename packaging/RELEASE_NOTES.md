# Windows 1.0.2：视频片尾修复与材料预检

对正式v2的11,087条视频逐条顺序解码至EOF，发现26条已独立核验的1–2帧片尾差异（情绪13、普通13），以及3条明确不完整的普通视频。其余11,058条声明帧数与本次解码计数相同。

本次修复Session3所报片尾问题，并兼容所有通过原文件SHA256和独立严格解码核验的1–2帧差异。保持原播放时长，记录兼容标记，不改变材料、题库、分组、评分或已有状态。

**仍需完整原片：v2 Session 20（6076.mp4）、24（2693.mp4）、33（6241.mp4）。这些原件与当前材料Release一致但确实不完整。安装包不能修复缺失内容；更新后在对应正式Session开始前停止提示，不强行跳过。**

已有1.0.1用户：正常保存、关闭采集并备份整个被试目录；把安装包复制到电脑本机后直接更新，不卸载、不重复迁移、不清空进度。主试独立检查后，用原编号和原Session续跑。失败当前片会重播并评分；重复暴露需记录，保留原attempt。

- 安装包：VisualVideoTask-Setup-Windows-x64.exe；自带环境，Windows10/11 x64，日常采集离线。
- SHA256SUMS.txt核对安装包，视频应与材料清单哈希比较，两者不能混比。
- README故障表已改为明确操作步骤；[完整问题清单与处理](https://github.com/18yiba/visual-video-task/blob/master/docs/operations/VIDEO_EOF_AUDIT.md)。
- 验证包括全库顺序解码、异常文件严格FFmpeg复核、旧/新播放器回归、真实窗口评分/续跑及模拟EEG断流。真实实验室EEG设备和物理时延仍未验收。
