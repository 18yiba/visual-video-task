# 2026-09-15 EEG断流保护维护记录

本地事故副本审计确认旧运行器存在静默断流：行为事件继续增长而EEG样本数长时间冻结。另一处traceback确认session_summary替换遭WinError 5，属于独立文件保存故障。个案路径、被试数据、原始事件和详细报告保留本地，不进入源码发布。

新增SampleWatchdog与类型化采集异常、独立健康线程、约每秒JSONL样本计数/到达速率/无数据时长、即时错误JSON和本地错误事件。默认启动10秒、已有样本后5秒超时。只监控本地样本增长，不猜测物理关机、电池或阻抗原因。

视频、答题和休息期间检查后台异常。断流中止的当前尝试不提交完成，保留续跑；错误后的offset只写本地并标记EEG不可用。警告在导出前立即显示，导出后保持显示供主试确认。冻结记录器阻止迟到的阻塞读取改写导出内容，SDK关闭异常不再直接阻止已有样本导出。

新增保留原协议的硬盘源码补丁：安装前校验/备份，仅替换白名单video_eeg Python源码；保留原配置、材料、题库、Session成员与状态，刷新已有离线包源码校验清单。不上传运行数据或环境。

旧内容题范式真实窗口回归通过：合成视频完整播放、原题库接口作答、进度提交及模拟EEG导出正常。
干净白名单源码副本测试99项通过、2项因未附带本地正式材料跳过。发布不增加安装依赖。

验证：101项自动测试通过，包含SDK读取阻塞时独立看门狗仍能报错且拒收迟到数据；真实PsychoPy模拟EEG窗口在视频、疲劳、唤醒和休息四个阶段注入断流均通过，并完成视频中断后续跑。故障截图检查后去掉过长完整路径，避免提示被裁切；完整路径输出终端。补丁首次与重复安装、源码备份、全部哈希、配置/题库/状态保全及离线清单校验通过。真实设备有电关机根因无法由旧文件确定；真实BrainCo断连与目标电脑验收待现场测试。
