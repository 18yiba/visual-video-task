# Formal AIGC image library

Place the 500+ images for the `formal500` experiment in this directory.

Maintain a separate `manifest.json` here. Its paths must be relative to this
directory, and every `id`/`image_id` must be unique. The formal experiment does
not scan or use images from `../pilot`.
